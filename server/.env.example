# Copy to .env (gitignored) and fill in. Generate the token with:
#   openssl rand -hex 32
# The same token goes into main/metrics_secrets.h on the firmware side.
INFLUX_ADMIN_PASSWORD=change-me-too
INFLUX_TOKEN=
